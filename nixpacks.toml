providers = ["node"]

[variables]
NODE_VERSION = "22.17.0"

[phases.setup]
nixPkgs = ["coreutils"]

[phases.build]
cmds = [
  "npm install -g corepack",
  "corepack enable",
  "corepack prepare pnpm@10.13.1 --activate"
]

[phases.install]
cmds = ["pnpm install"]